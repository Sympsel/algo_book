# 算法错误点统计

在多组测试用例输入时使用continue或者break，导致数据读取不完整并错位

数据溢出判断错误

dfs参数传反

不开long long见祖宗

if语句省略括号导致逻辑错误

if语句进行特判忘记continue

使用std::map存自定义类型没有写比较函数导致编译错误

使用自定义类型写了全参构造函数，忘记写默认构造或给缺省值0

把INF的值赋值给N，导致空间申请失败

使用其他题目的测试用例调试了几十分钟代码

双重循环内层终止变量m写成n

str.substr(x, y); 第二个参数应为截取长度，不是终止下标

区间dp j = n - i + 1的人才

字符串添加占位适应1-base索引，添加到尾部: `s += '_'`

进行高精度加法，去除前导0未保留个位

```cpp
// 智商太低的写不出来
int cmp(const string& s1, const string& s2) {
    int sz1(s1.size()), sz2(s2.size());
    if (sz1 == sz2) {
        for (int i{}; i < sz1; ++i) {
            if (s1[i] > s2[i]) return 1;
            else if (s1[i] < s2[i]) return -1;
            else{}
        }
        return 0;
    }
    return sz1 > sz2;
}
```

高精度乘法忘记使用`+=`

在变量初始化前使用变量

数据范围1e4定义N = 1004

二进制转整形出错
```cpp
int readln() {
    cin >> str;
    int ret{};
    for (int i{}; i < 5; ++i) {
        if (str[i] == '0') {
            // 实际应为 ret |= (1 << (4 - i));
            ret |= (1 << (4 + i));
        }
    }
    return ret;
}
```

数组没放全局没有且没有手动初始化

使用哈希表时忘记初始化哈希表 eg: `mp[0] = 0`

忘记读取输入

使用`#include<bits/stdc++.h>`与`using namespace std;`，并且使用`y1`与`cmath`中的函数命名冲突

归并时使用同一个数组的不同索引归并 `c[k++] = a[i++]; c[k++] = a[j++]`，应分别使用两个子区间

使用`string.find()`时，查找到起始索引后没有跳过，导致死循环

图论没有对输入权值取min

dfs忘记初始化入口数据

浮点数异常，除零错误

dfs参数错误导致跳过数据
```
void dfs(int idx) {
    if (ret < cnt) {
        retPath = path;
        ret = cnt;
    }
    for (int i{idx + 1}; i <= n; ++i) {
        if (!st[idx][i]) continue;
        path.emplace_back(i);
        cnt += w[i];
        dfs(i + 1); // 应为 dfs(i);
        path.pop_back();
        cnt -= w[i];
    }
}
```

sort函数边界出错

以下两段代码是有区别的
```
double ret{};
double sum{};
for (int i{1}; i <= n; ++i) {
    auto& [id, w]{a[i]};
    cout << id << " ";
    ret += sum;
    sum += w;
}
cout << "\n";
cout << fixed << setprecision(2) << ret / n << "\n";

ll ret{};
ll sum{};
for (int i{1}; i <= n; ++i) {
    auto& [id, w]{a[i]};
    cout << id << " ";
    ret += sum;
    sum += w;
}
cout << "\n";
cout << fixed << setprecision(2) << 1.f * ret / n << "\n";
```

内层修改循环变量导致死循环

```
void bin_enum() {
    for (int st{}; st < (1 << n); ++st) {
        int v{cnt1(st)};
        if (v >= ret) continue;
        memset(cur, 0, sizeof cur);
        path.clear();
        for  (int i{1}; st; ++i, st >>= 1) { // we move it
            if (st & 1) {
                path.emplace_back(i);
                for (int j{1}; j <= m; ++j) {
                    cur[j] += a[i][j];
                }
            }
        }
        for (int i{1}; i <= m; ++i) {
            if (cur[i] < tar[i]) goto next;
        }
        ret = v;
        retPath = path;
        next:;
    }
}
```

循环变量字母抄错

读取参数顺序错误`cin >> n >> m`

不开long long见祖宗，不要同时修改两份代码（平台上的，本地编辑器的），导致代码逻辑未及时更新

dp第二维遍历起始地点错误

未搞清题目要求的结果

未优先考虑贪心，使用dfs导致超时

默认输入有序未排序

写滑动窗口时未统计出现次数，仅标记是否出现

索引计算错误
```cpp
 for (int i{}; i < n; ++i) {
    for (int j{}; j < m; ++j) {
        cin >> _;
        // 实际上应为a[i * m + j + 1] = {_, i + 1};
        a[i * n + j + 1] = {_, i + 1};
    }
}
```

非法对数组排序贪心求解

当使用 `arr[i] = {i, a, t};` 这种聚合初始化时，初始化顺序严格按照结构体成员声明的顺序，而不是构造函数的参数顺序
~~c++求你别挖坑了，看了三十分钟都没发现问题~~

`q.size()` 与 `q.empty()`用反了

队列出队结构化绑定不要使用引用，或出队逻辑后置 ~~又被c++坑了~~
```
auto [u, v, w]{q.top()}; q.pop();

auto& [u, v, w]{q.top()}; q.pop(); // q.pop()后变量会被销毁，引用空引

// 使用
while (q.size()) {
    auto [u, v, w]{q.top()}; q.pop();
    // 逻辑
}

// 或使用
while (q.size()) {
    auto& [u, v, w]{q.top()};
    // 逻辑
    q.pop();
}
```

变量名错误

使用二分写代码时`check()`函数，容器放在全局或没有重置

局部变量覆盖全局变量
```
int n;

void test() {
    // use n
}

int main() {
    int n; cin >> n;
    // ...
}
```

用了错误的测试用例，找了很久bug

条件取反未变号
`(A && !B)` !=> `!(!A && B)`

冒泡排序写错的人才来咧
```cpp
for (int i{1}; i <= n; ++i) {
    for (int j{i}; j >= 2; --j) {      // int j{n} XXX
        if (a[j].first < a[j - 1].first) {
            swap(a[j], a[j - 1]);
        }
    }
}
```

分解质因数忘了考虑余数

读取整行字符串用`cin`，从空格截断

忘记初始化编号
```c++
struct node {
    int id, cnt, rec;
    node(int _id = 0, int _cnt = 0, int _rec = 0) : id(_id), cnt(_cnt), rec(_rec) {}
    bool operator<(const node& o) const {
        if (rec != o.rec) return rec > o.rec;
        if (cnt != o.cnt) return cnt > o.cnt;
        return id < o.id;
    }
};

// ...
for (int i{1}; i <= n; ++i) a[i].id = i;
```

参数填错，改了半天代码
```c++
int fx{fd(u)}, fy{fd(u)}; // fd(v)
```

最终堆的数组存储顺序 ≠ 输入顺序

最长上升子序列dp解法
f[i]表示以**i为结尾**的最长上升子序列，不是1-i之间的最长上升子序列，并且最终结果从f表中取最大，不是取f[n]
```c++
// 即
f[i] = 1
for j : 1 ~ i - 1
    if a[i] > a[j]
        f[i] = max(f[i], f[j] + 1)
```

拿改变后的变量作非法比较

题目要求输出：对每个被攻占的城市，如果它会改变整个国家的连通性，则输出`Red Alert: City k is lost!`，其中k是该城市的编号；
否则只输出`City k is lost.`即可。如果该国失去了最后一个城市，则增加一行输出`Game Over.`
我的输出：
```c++
if (cnt <= pre) cout << "City " << cur << " is lost.\n";
else cout << "Red Alert: City " << cur << " is lost.\n"; // 应为 is lost!
```
这检查个蛋啊

浮点数判零要使用其绝对值，且判断用`EPS`需要为`double`类型

使用long long类型解析浮点数输入

并查集屏蔽某些节点时，计算连通分量时未同步屏蔽
```cpp
bool query() {
    int nums; cin >> nums;
    memset(mask, 0, sizeof mask);
    for (int i{1}; i <= n; ++i) uf[i] = i;
    for (int i{1}; i <= nums; ++i) {
        int _; cin >> _;
        mask[_] = true;
    }
    for (auto& [u, v] : ed) {
        if (mask[u] || mask[v]) continue;
        un(u, v);
    }
    // 计算联通分量
    int cnt{};
    for (int i{1}; i <= n; ++i) {
        if (uf[i] == i) ++cnt;
        // 应为 if (uf[i] == i && !mask[i]) ++cnt;
    }
    return cnt == n - nums;
}
```

外层循环和内层循环用同一个变量

求最小值未初始化变量为INF

漏看题目条件：仅一次

没读取完输入就break

`auto& [w, v] : ed[u]`邻接表解包顺序出错